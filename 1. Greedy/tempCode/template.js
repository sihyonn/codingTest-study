/**
 * vscode 풀이용
 */
//입력정제
const fs = require('fs');
let input = fs.readFileSync('./input.txt').toString().split('\n');

//문제풀이
function solution(test1, test2) {}

solution(test1, test2);

/**
 * 백준 제출용
 */
const fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().trim().split('\n');
